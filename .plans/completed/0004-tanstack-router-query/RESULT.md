# Result — 0004 tanstack-router-query

Status: **ready for UI review** (autonomy: review).

## What changed

### Added

- `web/src/routes/__root.tsx` — root route with `<Outlet/>` and dev-only TanStack Router + React Query devtools.
- `web/src/routes/index.tsx` — `/` file-route rendering "tempo" headline, tagline, and a shadcn `<Button>`.
- `web/src/routeTree.gen.ts` — generated by `@tanstack/router-plugin/vite` (committed; regenerated on every dev/build).

### Modified

- `web/vite.config.ts` — added `tanstackRouter({ target: "react", autoCodeSplitting: true })` as the **first** plugin (must precede `react()`).
- `web/src/main.tsx` — replaced `<App/>` with `<RouterProvider router={router}/>` wrapped in `<QueryClientProvider>`. `<ThemeProvider>` is preserved so `d`-key dark-mode toggle still works.
- `web/package.json` / `pnpm-lock.yaml` —
  - deps: `@tanstack/react-router`, `@tanstack/react-query`
  - devDeps: `@tanstack/router-plugin`, `@tanstack/router-devtools`, `@tanstack/react-query-devtools`

### Removed

- `web/src/App.tsx` — superseded by the index route component.
- `web/src/assets/react.svg` — orphaned starter asset, no remaining references.

## Verify output

```
$ ./verify.sh
verify ok
```

Underlying steps that ran:
- `pnpm -C web install --frozen-lockfile`
- `pnpm -C web build` → `tsc -b && vite build` — both pass.
- Bundle output: `dist/assets/index-*.js` 306.66 kB (gzip 96.98 kB), `dist/index.html` 0.46 kB.

## How to view it locally

```
pnpm -C web dev
```

Then open <http://localhost:5173/>. You should see:

- Centered "tempo" headline + tagline + Button labelled "Hello".
- Press `d` (no modifiers, not in an input) to toggle dark mode.
- TanStack Router devtools panel anchor: bottom-right.
- React Query devtools button anchor: bottom-left.

## Review checklist (please confirm)

- [ ] Index page renders the headline, tagline, and Button with the preset's button styling.
- [ ] TanStack Router devtools panel opens (bottom-right).
- [ ] React Query devtools button is present (bottom-left).
- [ ] Dark-mode toggle (`d`) still works.
- [ ] No console errors / warnings.

## Aesthetic concerns to consider

- **Spacing/rhythm:** is `gap-6` between headline / tagline / button comfortable, or too tight/loose?
- **Hierarchy:** is the size jump from `text-4xl` headline → muted-foreground tagline strong enough?
- **Button placement:** the button is the loudest element on a near-empty page — do you want it as an `outline` or a different variant for the placeholder, or is the default fine since this screen will be replaced shortly?
- **Devtools clutter:** dev-only floating buttons can crowd the corners — flag if you want them disabled by default.
- **Empty-screen feel:** acceptable as a routing smoke test, or do you want a slightly more deliberate placeholder before 0005 (embedding) lands?

## Followups

- 0005 (`embed-spa-in-binary`) consumes `web/dist`, so the build output verified here is what the Go binary will embed.
- The `routeTree.gen.ts` is committed per task notes — fresh clones can `pnpm build` without first running `pnpm dev`.

When satisfied, run `/finish-task 0004`.
